# PureLLM Project Notes

## Environment
- The user runs **vLLM** (NOT LM Studio) as their local LLM backend for this Home Assistant custom conversation integration.
- The integration entity name in HA is `conversation.purellm_lm_studio_local` (legacy naming, but backend is vLLM).
